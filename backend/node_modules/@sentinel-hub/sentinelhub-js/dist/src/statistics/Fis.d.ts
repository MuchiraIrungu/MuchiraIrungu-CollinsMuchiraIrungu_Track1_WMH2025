import type { AbstractLayer } from '../layer/AbstractLayer';
import type { AbstractSentinelHubV3Layer } from '../layer/AbstractSentinelHubV3Layer';
import { FisResponse, GetStatsParams } from '../layer/const';
import { RequestConfiguration } from '../utils/cancelRequests';
import type { StatisticsProvider } from './StatisticsProvider';
export declare class Fis implements StatisticsProvider {
    private createFISPayload;
    private convertFISResponse;
    getStats(layer: AbstractLayer, _params: GetStatsParams, reqConfig?: RequestConfiguration): Promise<FisResponse>;
    handleV3(layer: AbstractSentinelHubV3Layer, params: GetStatsParams, reqConfig?: RequestConfiguration): Promise<FisResponse>;
}
